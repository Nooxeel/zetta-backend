# ─── Server ──────────────────────────────────────
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# ─── Database Connections (SQL Server / SoMee) ───
# Each database follows the pattern: DB_{NAME}_{FIELD}
# Register database names in src/index.ts or a config file.
#
# Example for a database called "INVENTARIO":
# DB_INVENTARIO_SERVER=sql.somee.com
# DB_INVENTARIO_DATABASE=myInventarioDB
# DB_INVENTARIO_USER=myuser
# DB_INVENTARIO_PASSWORD=mypassword
# DB_INVENTARIO_PORT=1433
#
# Example for a database called "VENTAS":
# DB_VENTAS_SERVER=sql.somee.com
# DB_VENTAS_DATABASE=myVentasDB
# DB_VENTAS_USER=myuser
# DB_VENTAS_PASSWORD=mypassword

# ─── PostgreSQL (ETL warehouse) ──────────────────
POSTGRES_URL=postgresql://zetta:zetta_dev_2024@localhost:5432/zetta_reports
# Docker Compose overrides (optional):
POSTGRES_USER=zetta
POSTGRES_PASSWORD=zetta_dev_2024
POSTGRES_DB=zetta_reports
POSTGRES_PORT=5432
# pgAdmin (optional, only with: docker compose --profile tools up)
PGADMIN_EMAIL=admin@zetta.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ─── Authentication (Google OAuth + JWT) ──────────
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
JWT_SECRET=your-random-secret-at-least-32-chars
